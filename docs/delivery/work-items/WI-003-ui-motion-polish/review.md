---
id: WI-003
date: 2026-04-09
status: done
---

## 完成情况

- 已重写主题初始化脚本，确保首屏按本地偏好或系统偏好应用主题
- 已修复主题按钮的点击切换逻辑和本地持久化逻辑
- 已补齐 `color-scheme` 与 `.dark` 样式生效链路
- 已通过 `lint` 和 `build` 验证

## 根因总结

- `ThemeToggle` 中存在断裂代码，`matchMedia` 监听逻辑被损坏
- 组件在 `effect` 中直接同步 `setState`，触发了 React 新规则的 lint 错误
- 初始化脚本只处理了加深色类，没有形成完整的深浅色闭环

## 当前结果

- 当前浏览器中点击主题按钮会立即切换页面主题
- 刷新页面后会保留上次主题选择
- 未设置本地偏好时，仍会跟随系统主题变化
